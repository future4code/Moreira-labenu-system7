export default class turma{
    public id: string;
    public name: string;
    public modulo: string
    constructor(id: string, name: string,  modulo: string){
        this.id = id,
        this.name = name,
        this.modulo = modulo
     
    }
}